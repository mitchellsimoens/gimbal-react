import React from 'react';
import Table from '../../components/Table';
import { Attributes } from '../../../typings';

const HeapSnapshot = ({ data }: Attributes) => <Table data={data} />;

export default HeapSnapshot;
